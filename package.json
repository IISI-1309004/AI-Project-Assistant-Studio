{
  "name": "aipa-studio",
  "private": true,
  "version": "1.0.0",
  "description": "AI Project Assistant Studio — Enterprise AI Development Platform",
  "workspaces": [
    "cli",
    "web",
    "plugin/vscode"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
